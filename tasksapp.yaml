apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasksapp
  labels:
    app: tasksapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tasksapp
  template:
    metadata:
      labels:
        app: tasksapp
    spec:
      containers:
        - name: tasksapp
          image: zivamram/tasksapp-python:${{ env.IMAGE_TAG_WITH_RUN_ID }}
          ports:
            - containerPort: 5000
          imagePullPolicy: Always
